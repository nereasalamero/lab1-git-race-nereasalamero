/**
 * Function that saves the language when the user changes it.
 */
document.querySelectorAll(".lang-switch").forEach(link => {
    link.addEventListener("click", function (e) {
        e.preventDefault();
        const lang =
    })
})